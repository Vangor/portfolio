# portfolio .env.local template — secrets resolve from 1Password.
# Materialize live file with: bun run hydrate (after /si lands the hydrate flow)
# or manually: op inject -f -i .env.local.tmpl -o .env.local

VITE_POSTHOG_KEY=op://4etverg/Portfolio PostHog/api_key
VITE_POSTHOG_HOST=https://us.i.posthog.com
