# PostHog analytics host (public URL — no secret key, safe to share)
VITE_POSTHOG_HOST=https://us.i.posthog.com

# Production site URL (used by Next.js artefact — can be removed once README is updated)
# NEXT_PUBLIC_SITE_URL=https://your-domain.example
