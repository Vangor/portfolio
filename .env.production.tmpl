# portfolio .env.production template — secrets resolve from 1Password.
# Materialize live file with: bun run hydrate (after /si lands the hydrate flow)
# or manually: op inject -f -i .env.production.tmpl -o .env.production

# Site URL in production (will be set by Cloudflare)
NEXT_PUBLIC_SITE_URL=https://konstantin@vanichk.in

# Any other environment variables your app needs in production
NODE_ENV=production 

NEXT_PUBLIC_POSTHOG_KEY=op://4etverg/Portfolio PostHog/api_key
